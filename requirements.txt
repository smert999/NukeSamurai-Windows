# NukeSamurai - Required Python Packages
# 
# IMPORTANT: This file is for SYSTEM PYTHON packages (for SAM2 inference in subprocess)
# 
# Additionally, you need to install opencv-python and numpy in NUKE PYTHON:
#   "C:\Program Files\Nuke16.0v4\python.exe" -m pip install opencv-python numpy
# 
# Why two installations?
#   - System Python: torch, SAM2 dependencies (for mask generation via subprocess)
#   - Nuke Python: opencv-python, numpy (for Bounding Box GUI with cv2.selectROI)

# Install with: pip install -r requirements.txt

# PyTorch with CUDA (choose one based on your CUDA version)
# For CUDA 12.1:
# torch==2.5.1+cu121
# torchvision==0.20.1+cu121
# Install separately: pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121

# For CUDA 11.8:
# torch==2.5.1+cu118
# torchvision==0.20.1+cu118
# Install separately: pip install torch==2.5.1+cu118 torchvision==0.20.1+cu118 --index-url https://download.pytorch.org/whl/cu118

# Core dependencies
# Note: NumPy 2.x is safe! Subprocess architecture isolates us from Nuke's NumPy issues
numpy>=1.26.0
opencv-python>=4.8.0
Pillow>=10.0.0

# SAM2 dependencies
hydra-core>=1.3.0
omegaconf>=2.3.0
iopath>=0.1.10
timm>=0.9.0
tqdm>=4.65.0
einops>=0.7.0
loguru>=0.7.0

# Optional but recommended
matplotlib>=3.7.0

