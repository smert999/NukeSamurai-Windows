# 🚀 Последние Улучшения NukeSamurai

## ✅ Исправлено: Read Node Создание (v1.1)

### Проблема
Read нода не создавалась автоматически после генерации масок, если путь содержал формат `%04d` или `%03d`.

### Решение
Теперь плагин корректно распознаёт все форматы последовательностей:
- `%04d` (например: `render_%04d.png` → `render_0001.png, render_0002.png...`)
- `%03d` (например: `mask_%03d.png` → `mask_001.png, mask_002.png...`)
- `####` (Nuke-стиль: `output_####.png`)
- `###` (Nuke-стиль: `output_###.png`)

**Теперь Read нода создаётся автоматически** справа от узла SAMURAI для всех форматов последовательностей!

---

## 📊 Улучшено: Детальный Прогресс-Бар

### Что Нового
Прогресс-бар теперь показывает **детальную информацию о текущем этапе** обработки:

#### **Этап 1: Loading Model...**
Загрузка весов модели SAM 2.1 на GPU.

#### **Этап 2: Initializing Video...**
Подготовка видеопредиктора для обработки.

#### **Этап 3: Reading Frames...**
Чтение кадров из последовательности изображений.

#### **Этап 4: Detecting Object...**
Первичная детекция объекта на базовом кадре по Bounding Box.

#### **Этап 5: Propagating Masks...**
Пропагация маски на все кадры последовательности:
- **Детальная информация**: `Frame 1001/1182 (45%)`
- **Процент выполнения**: Обновляется в реальном времени
- **Номер кадра**: Показывает текущий и конечный кадр

#### **Этап 6: Saving Complete!**
Все маски успешно сохранены.

### Преимущества
- ✅ **Прозрачность**: Вы всегда знаете, что происходит в данный момент
- ✅ **Контроль**: Можно отменить процесс на любом этапе (нажатием "Cancel")
- ✅ **Точность**: Видите номера конкретных кадров, которые обрабатываются
- ✅ **Производительность**: Нет "зависаний" — каждый этап имеет индикацию

---

## 🎯 Добавлено: Reference Frame Selector (v1.2)

### Что Это?
**Reference Frame Selector** — новая функция, позволяющая выбрать **любой кадр** в последовательности для разметки объекта, а не только первый!

### Зачем?

**Раньше (v1.1)**:
- ❌ Bounding Box создавался **только на первом кадре** (`FrameRangeMin`)
- ❌ Если объект скрыт/размыт/обрезан на первом кадре → плохой трекинг
- ❌ Приходилось менять Frame Range, чтобы "удобный" кадр был первым

**Теперь (v1.2)**:
- ✅ Выбирайте **любой кадр**, где объект виден лучше всего
- ✅ SAM2 пропагирует маску в **обе стороны** от reference frame
- ✅ Больше не нужно изменять Frame Range

### Как Использовать

1. **Настройте Frame Range**: `1001 - 1200`
2. **Выберите Reference Frame**: Например, `1085` (где объект виден лучше всего)
3. **Create Bounding Box**: OpenCV откроет кадр **1085** для разметки
4. **Generate Mask**: SAM2 отследит объект на всех кадрах (1001-1200)

### Примеры Использования

#### **Пример 1: Объект появляется в середине сцены**
```
Frame Range:      1001 - 1200
Reference Frame:  1050    ← Человек полностью виден здесь
```
✅ Маска пропагирована от 1050 в обе стороны

#### **Пример 2: Объект размыт на первом кадре**
```
Frame Range:      1001 - 1182
Reference Frame:  1090    ← Объект чёткий на этом кадре
```
✅ Размечаете чёткий кадр, SAM2 отследит даже размытые

#### **Пример 3: Частичная окклюзия**
```
Frame Range:      1001 - 1200
Reference Frame:  1120    ← Объект полностью виден
```
✅ Даже если объект скрыт в начале, трекинг работает

### Преимущества
- ✅ **Гибкость**: Любой кадр для разметки
- ✅ **Точность**: Выбирайте кадр с лучшей видимостью
- ✅ **Удобство**: Не нужно менять Frame Range
- ✅ **Производительность**: SAM2 оптимально пропагирует от лучшего кадра

### UI Изменения

Новое поле в узле SAMURAI:

```
┌──────────────────────────────────────┐
│ File Path:        [________________] │
│ [Update Path]                        │
│                                       │
│ Reference Frame:  [1050]  ← НОВОЕ!   │
│ [Create Bounding Box]                │
│                                       │
│ Frame Range:      [1001] - [1200]    │
│ FPS:              [24]               │
│ Model Type:       [Base+]            │
│ ...                                   │
└──────────────────────────────────────┘
```

**Tooltip**: "Frame number where you want to mark the object (can be any frame within Frame Range)"

### Валидация

Плагин автоматически проверяет:
```
Reference Frame ≥ FrameRangeMin
Reference Frame ≤ FrameRangeMax
```

Если Reference Frame вне диапазона:
```
⚠️ Ошибка!

Reference Frame (900) должен быть в пределах Frame Range (1001 - 1200)
```

### Прогресс-Бар

Теперь показывает номер reference frame:
```
Detecting Object on Frame 1085...  ← Видите, на каком кадре идёт детекция
```

### Подробности

Полное руководство: **`REFERENCE_FRAME_GUIDE.md`**

---

## 🔧 Технические Детали

### Изменённые Файлы
1. **`scripts/nuke_samurai.py`**:
   - Улучшен парсинг вывода из `sam2_worker.py`
   - Добавлена поддержка `STAGE:` сообщений для обновления текста прогресс-бара
   - Исправлена проверка форматов последовательностей для автоматического создания Read ноды

2. **`scripts/sam2_worker.py`**:
   - Добавлены `STAGE:` сообщения на каждом этапе обработки
   - Улучшена детализация в `STAGE:Frame X/Y (Z%)` для этапа пропагации
   - Исправлен расчёт `total_frames` для корректного процента выполнения

### Формат Протокола Subprocess
```
STAGE:название_этапа        # Текущий этап (показывается в прогресс-баре)
PROGRESS:процент            # Процент выполнения (0-100)
[SAM2 Worker] лог-сообщение # Детальный лог в консоль Nuke
```

---

## 📝 Использование

### Пример 1: Создание Read Ноды
```
Output Path: D:/renders/mask_%04d.png
```
✅ **Результат**: После генерации масок автоматически создаётся Read нода с путём `D:/renders/mask_%04d.png`, расположенная **справа от узла SAMURAI**.

### Пример 2: Отслеживание Прогресса
Во время обработки вы увидите в прогресс-баре:

```
SAM2 Propagation (GPU)
Loading Model...                    [====----] 10%
Reading Frames...                   [========] 30%
Detecting Object...                 [=========] 40%
Frame 1045/1182 (65%)              [==============----] 65%
Saving Complete!                    [==================] 100%
```

---

## 🎯 Что Дальше?

### Планируемые Улучшения (v2.0)
- 🧠 **Интеграция SAM 3.0**: Поддержка текстовых промптов ("red car", "person in blue shirt")
- 📦 **Batched Inference**: Ускоренная обработка для последовательностей
- 🎨 **Multi-Object Tracking**: Отслеживание нескольких объектов одновременно
- 💾 **Session Management**: Сохранение и загрузка сессий трекинга

---

## 📅 История Изменений

### v1.2 (20.11.2025)
- 🎯 **Добавлено**: Reference Frame Selector — выбирайте любой кадр для разметки объекта
- ✨ Улучшено: Прогресс-бар показывает номер reference frame при детекции
- 📚 Добавлено: Полное руководство `REFERENCE_FRAME_GUIDE.md`
- 🔧 Техническое: Передача `reference_frame_idx` в SAM2 worker

### v1.1 (20.11.2025)
- ✨ Добавлен детальный прогресс-бар с этапами
- 🐛 Исправлено: Read нода не создавалась для `%04d` и `%03d` форматов
- 📊 Улучшена видимость текущего состояния обработки

### v1.0 (19.11.2025)
- 🎉 Начальный релиз
- ✅ GPU-ускорение с RTX 4090
- ✅ Subprocess-архитектура для изоляции PyTorch
- ✅ Поддержка SAM 2.1 (Base+, Large, Small, Tiny)
- ✅ Автоматическое создание Read ноды

---

## 🙏 Обратная Связь

Если у вас есть предложения или вы обнаружили ошибки, пожалуйста, создайте Issue на GitHub:
[https://github.com/YOUR_USERNAME/NukeSamurai/issues](https://github.com/YOUR_USERNAME/NukeSamurai/issues)

**Спасибо за использование NukeSamurai!** 🎬✨

