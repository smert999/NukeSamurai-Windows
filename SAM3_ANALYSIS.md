# 🚀 SAM 3: Революция в сегментации - Детальный анализ

> **Meta AI представила SAM 3 - следующее поколение Segment Anything Model**  
> Официальный релиз: Ноябрь 2025  
> Репозиторий: https://github.com/facebookresearch/sam3  
> Сайт: https://ai.meta.com/sam3/

---

## 📖 Оглавление

1. [Что такое SAM 3?](#что-такое-sam-3)
2. [Ключевые улучшения](#ключевые-улучшения)
3. [Архитектура](#архитектура)
4. [Новые возможности](#новые-возможности)
5. [Сравнение с SAM 2.1](#сравнение-с-sam-21)
6. [Результаты на бенчмарках](#результаты-на-бенчмарках)
7. [Применение в VFX](#применение-в-vfx)
8. [Интеграция в NukeSamurai](#интеграция-в-nukesamurai)
9. [Технические требования](#технические-требования)
10. [Заключение](#заключение)

---

## 🎯 Что такое SAM 3?

**SAM 3 (Segment Anything Model 3)** - это третье поколение модели сегментации от Meta AI. В отличие от предыдущих версий, SAM 3 вводит концепцию **"Segment Anything with Concepts"** - сегментацию через концептуальное понимание.

### Основные характеристики:

- **848M параметров** (на 26% больше, чем SAM 2.1)
- **Text-based prompting** как основной метод взаимодействия
- **Visual exemplar prompts** - сегментация по примеру объекта
- **Unified architecture** для изображений и видео
- **Открытый код** под Apache 2.0 License

---

## 🔥 Ключевые улучшения

### 1. **Text Prompts - Революция в workflow** 🎯

Это главное нововведение SAM 3. Теперь вместо рисования bounding box или точек, можно просто **написать текстом** что нужно найти.

#### Примеры:

**Простые запросы:**
```python
"car"           # Все машины на изображении
"person"        # Все люди
"tree"          # Все деревья
"building"      # Все здания
```

**Запросы с атрибутами:**
```python
"red car"              # Только красные машины
"person in blue shirt" # Человек в синей рубашке
"large tree"           # Большие деревья
"tall building"        # Высокие здания
```

**Контекстные запросы:**
```python
"car on the road"      # Машина именно на дороге
"person walking"       # Идущий человек (не стоящий)
"bird in the sky"      # Птица в небе
"dog near the house"   # Собака возле дома
```

**Релятивные запросы:**
```python
"car in front of the building"  # Пространственные отношения
"person behind the tree"        # Позиция относительно других
"cup on the table"              # Объект на поверхности
```

### 2. **Visual Exemplar Prompts** 🖼️

Новая концепция: показываете модели **пример объекта**, и она находит все похожие.

**Use cases:**
- **Однородные объекты:** Выделили один стул → модель находит все 100 стульев
- **Паттерны:** Показали элемент узора → модель сегментирует весь паттерн
- **Уникальные объекты:** Показали часть объекта → модель находит весь объект

### 3. **Improved Video Tracking** 🎬

SAM 3 наследует архитектуру трекера SAM 2, но с улучшениями:

- **Промпты на любом кадре** (не только на первом)
- **Множественные промпты** в одной сессии
- **Интерактивное уточнение** во время обработки
- **Session management** для сложных сценариев

### 4. **SAM 3 Agent** 🤖

Специальный режим для обработки **сложных композитных запросов**:

```python
"all people wearing red shirts and standing near the car"
```

Модель самостоятельно:
1. Находит всех людей
2. Фильтрует тех, кто в красной одежде
3. Фильтрует тех, кто рядом с машиной

### 5. **Negative Prompts** ❌

Возможность указывать что **НЕ** нужно сегментировать:

```python
"not person in red"     # Всё кроме человека в красном
"not background"        # Всё кроме фона
"all cars except red"   # Все машины кроме красных
```

---

## 🏗️ Архитектура

### Компоненты SAM 3:

```
┌─────────────────────────────────────────────────────────┐
│                      SAM 3 Model                        │
│                      (848M params)                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────┐         ┌──────────────────┐    │
│  │  Vision Encoder  │ (shared)│  Vision Encoder  │    │
│  │   (Hiera-L)      │────────>│   (Hiera-L)      │    │
│  └──────────────────┘         └──────────────────┘    │
│          │                             │               │
│          v                             v               │
│  ┌──────────────────┐         ┌──────────────────┐    │
│  │  DETR Detector   │         │  SAM 2 Tracker   │    │
│  │  - Text cond.    │         │  - Memory attn.  │    │
│  │  - Geometry      │         │  - Temporal      │    │
│  │  - Exemplars     │         │  - Interactive   │    │
│  └──────────────────┘         └──────────────────┘    │
│          │                             │               │
│          v                             v               │
│  ┌──────────────────┐         ┌──────────────────┐    │
│  │  Image Results   │         │  Video Results   │    │
│  │  - Masks         │         │  - Tracked masks │    │
│  │  - Boxes         │         │  - Consistency   │    │
│  │  - Scores        │         │  - Refinement    │    │
│  └──────────────────┘         └──────────────────┘    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Ключевые особенности:

1. **Shared Vision Encoder (Hiera-L)**
   - Единый энкодер для изображений и видео
   - Эффективная архитектура Hierarchical Vision Transformer

2. **DETR-based Detector**
   - Conditioning на три типа промптов:
     - Text (новое!)
     - Geometry (bbox, points)
     - Visual exemplars (новое!)
   - End-to-end обучение

3. **SAM 2 Tracker (наследуется)**
   - Memory attention mechanism
   - Cross-frame consistency
   - Interactive refinement

---

## 🎨 Новые возможности

### 1. Text-based Image Segmentation

```python
from sam3.model_builder import build_sam3_image_model
from sam3.model.sam3_image_processor import Sam3Processor
from PIL import Image

# Загрузка модели
model = build_sam3_image_model()
processor = Sam3Processor(model)

# Загрузка изображения
image = Image.open("scene.jpg")
inference_state = processor.set_image(image)

# TEXT PROMPT!
output = processor.set_text_prompt(
    state=inference_state,
    prompt="red car"
)

# Результаты
masks = output["masks"]      # Маски найденных объектов
boxes = output["boxes"]      # Bounding boxes
scores = output["scores"]    # Confidence scores
```

### 2. Text-based Video Segmentation

```python
from sam3.model_builder import build_sam3_video_predictor

# Создание предиктора
video_predictor = build_sam3_video_predictor()

# Старт сессии
response = video_predictor.handle_request(
    request=dict(
        type="start_session",
        resource_path="video.mp4"
    )
)
session_id = response["session_id"]

# Добавление text prompt
response = video_predictor.handle_request(
    request=dict(
        type="add_prompt",
        session_id=session_id,
        frame_index=0,  # Можно на любом кадре!
        text="person walking"
    )
)

# Результаты для всех кадров
outputs = response["outputs"]
```

### 3. Visual Exemplar Prompting

```python
# Вырезаем пример объекта
exemplar_crop = image[y:y+h, x:x+w]

# SAM 3 находит все похожие
output = processor.set_exemplar_prompt(
    state=inference_state,
    exemplar=exemplar_crop
)
```

### 4. Multi-object Session

```python
# Одна сессия - несколько объектов
session = video_predictor.start_session("video.mp4")

# Объект 1
video_predictor.add_prompt(session, frame=0, text="person in blue")

# Объект 2
video_predictor.add_prompt(session, frame=0, text="red car")

# Объект 3
video_predictor.add_prompt(session, frame=50, text="dog")

# Все маски обрабатываются одновременно!
```

### 5. Batched Inference

```python
# Обработка множества изображений
images = [img1, img2, img3, ..., imgN]
prompts = ["car", "person", "tree", ...]

outputs = processor.batch_inference(
    images=images,
    prompts=prompts,
    batch_size=8  # GPU оптимизация
)
```

---

## 📊 Сравнение с SAM 2.1

| Параметр | SAM 2.1 | SAM 3 | Улучшение |
|----------|---------|-------|-----------|
| **Параметры модели** | ~670M | 848M | +26% |
| **Text prompts** | ❌ | ✅ | Новое! |
| **Bbox/Point prompts** | ✅ | ✅ | Сохранено |
| **Visual exemplars** | ❌ | ✅ | Новое! |
| **Negative prompts** | ❌ | ✅ | Новое! |
| **Multi-object tracking** | Ограничено | ✅ | Улучшено |
| **Session management** | ❌ | ✅ | Новое! |
| **Agent mode** | ❌ | ✅ | Новое! |
| **Batched inference** | ❌ | ✅ | Новое! |
| **LVIS cgF1** | - | 37.2 | Benchmark |
| **SA-Co/Gold cgF1** | - | 54.1 | Benchmark |
| **COCO AP** | - | 53.6 | Benchmark |

---

## 🏆 Результаты на бенчмарках

### Изображения (Instance Segmentation)

| Model | LVIS cgF1 | SA-Co/Gold cgF1 | COCO AP |
|-------|-----------|-----------------|---------|
| **Human** | - | **72.8** | - |
| OWLv2 | 29.3 | 24.6 | 45.5 |
| DINO-X | - | 21.3 | 52.4 |
| Gemini 2.5 | 13.4 | 13.0 | - |
| **SAM 3** | **37.2** | **54.1** | **53.6** |

**Выводы:**
- ✅ SAM 3 лучше всех существующих моделей
- ✅ На 74% пути к человеческому уровню (SA-Co)
- ✅ Превосходит DINO-X и Gemini 2.5

### Видео (Video Object Segmentation)

| Model | SA-V test | YT-Temporal-1B | SmartGlasses | BURST HOTA |
|-------|-----------|----------------|--------------|------------|
| **Human** | 53.1 | 71.2 | 58.5 | - |
| **SAM 3** | 30.3 | 50.8 | 36.4 | 44.5 |

**Выводы:**
- ✅ Хорошие результаты на сложных сценах
- ✅ ~71% от человеческого уровня (YT-Temporal)
- ⚠️ Есть пространство для улучшений

### Box Detection

| Model | COCO AP | LVIS AP | APo (COCO-O) |
|-------|---------|---------|--------------|
| OWLv2 | 46.1 | 43.4 | 23.9 |
| DINO-X | 56.0 | 38.5 | - |
| **SAM 3** | **56.4** | **48.5** | **55.7** |

---

## 🎬 Применение в VFX

### Сценарии использования:

#### 1. **Rotoscoping / Mask Generation** 🎭

**Традиционный workflow (SAM 2):**
```
1. Найти объект в кадре
2. Нарисовать bbox
3. Запустить трекинг
4. Проверить результат
5. Исправить ошибки вручную
```

**Новый workflow (SAM 3):**
```
1. Написать: "person in blue jacket"
2. Запустить
3. Готово!
```

**Экономия времени:** ~70%

#### 2. **Multi-object Tracking** 👥

Одновременная сегментация нескольких объектов:

```python
# Один проход - все объекты
prompts = [
    "main character",
    "car in background",
    "flying bird",
    "dog on the street"
]

# SAM 3 обрабатывает всё за один раз
```

#### 3. **Asset Extraction** 📦

Извлечение объектов для 3D reconstruction:

```python
# Точная сегментация для photogrammetry
prompt = "entire statue without pedestal"

# SAM 3 понимает "entire" и "without"
```

#### 4. **Complex Scene Segmentation** 🏙️

Сегментация по сложным критериям:

```python
"all windows on the left building"
"cars moving from left to right"
"people in the foreground but not in the crowd"
```

#### 5. **Cleanup / Paint-out** 🎨

Быстрая маскировка для удаления:

```python
"all wires and cables"
"modern elements in historical scene"
"tracking markers"
```

---

## 🔧 Интеграция в NukeSamurai

### Текущий статус:

**NukeSamurai v1.0:**
- ✅ SAM 2.1 + SAMURAI
- ✅ Bbox prompting
- ✅ GPU acceleration (RTX 4090)
- ✅ ~15-20 сек для 181 кадра

### Планируемая интеграция SAM 3:

#### Фаза 1: Базовая интеграция (1-2 недели)

**Добавить в UI:**
```python
# Выбор модели
s.addKnob(nuke.Enumeration_Knob(
    'ModelVersion',
    'Model',
    ['SAM 2.1', 'SAM 3.0']
))

# Выбор типа промпта
s.addKnob(nuke.Enumeration_Knob(
    'PromptType',
    'Prompt Type',
    ['Bounding Box', 'Text Prompt', 'Visual Exemplar']
))

# Поле для text prompt
s.addKnob(nuke.String_Knob('TextPrompt', 'Text'))
```

**Обновить sam2_worker.py:**
```python
if model_version == "SAM3":
    from sam3.model_builder import build_sam3_video_predictor
    
    predictor = build_sam3_video_predictor()
    
    if prompt_type == "text":
        response = predictor.handle_request(
            request=dict(
                type="add_prompt",
                text=text_prompt,
                frame_index=start_frame
            )
        )
```

#### Фаза 2: Расширенные возможности (2-4 недели)

**Multi-object support:**
```python
# Множественные text prompts
s.addKnob(nuke.Multiline_Eval_String_Knob(
    'TextPrompts',
    'Text Prompts (one per line)'
))

# Каждый промпт = отдельная маска
prompts = text_prompts.split('\n')
for i, prompt in enumerate(prompts):
    mask_output = f"{output_path}_obj{i}_%04d.png"
```

**Visual exemplar prompts:**
```python
# Crop tool для выбора примера
s.addKnob(nuke.XY_Knob('ExemplarCrop', 'Exemplar Area'))

# SAM 3 находит похожие объекты
```

**Interactive refinement:**
```python
# Предварительный просмотр
s.addKnob(nuke.PyScript_Knob('Preview', 'Preview Result'))

# Уточнение точками
s.addKnob(nuke.MultiView_Knob('RefinePoints', 'Refine'))
```

#### Фаза 3: Оптимизация (1-2 недели)

- Кэширование моделей
- Batched inference для последовательностей
- Оптимизация VRAM usage
- Progress bar с детализацией

### Ожидаемые улучшения:

| Метрика | SAM 2.1 | SAM 3 (ожидается) |
|---------|---------|-------------------|
| **Workflow время** | 100% | ~30% (text prompts!) |
| **Ручные исправления** | Часто | Редко |
| **Multi-object** | Последовательно | Параллельно |
| **Качество масок** | Отлично | Лучше |
| **Inference время** | 15-20 сек | ? (тестировать) |

---

## 💻 Технические требования

### Минимальные:

- **Python:** 3.10+
- **PyTorch:** 2.7.0+
- **CUDA:** 12.6
- **GPU:** NVIDIA с CUDA support
- **VRAM:** 8 GB (минимум)
- **RAM:** 16 GB

### Рекомендуемые:

- **Python:** 3.12
- **PyTorch:** 2.7.0
- **CUDA:** 12.6
- **GPU:** RTX 4090 или аналогичная
- **VRAM:** 24 GB
- **RAM:** 32 GB
- **SSD:** Для кэша

### Установка:

```bash
# 1. Создание окружения
conda create -n sam3 python=3.12
conda activate sam3

# 2. Установка PyTorch
pip install torch==2.7.0 torchvision torchaudio \
    --index-url https://download.pytorch.org/whl/cu126

# 3. Клонирование репозитория
git clone https://github.com/facebookresearch/sam3.git
cd sam3

# 4. Установка SAM 3
pip install -e .

# 5. HuggingFace authentication
pip install huggingface_hub
huggingface-cli login  # Ввести access token
```

### Скачивание моделей:

Модели доступны на [HuggingFace](https://huggingface.co/facebook/sam3).

**Требуется:**
1. Запросить доступ на HF repo
2. Создать access token
3. Аутентифицироваться: `huggingface-cli login`

---

## 🔮 Заключение

### SAM 3 - это не просто улучшение, это новая парадигма!

**Ключевые преимущества:**

1. **Text prompts** 🔥
   - Революционное упрощение workflow
   - Интуитивное взаимодействие
   - Понимание контекста и атрибутов

2. **Visual exemplars** 🖼️
   - "Найди похожее"
   - Работа с паттернами и текстурами
   - Однородные объекты за один клик

3. **Improved architecture** 🏗️
   - 848M параметров
   - Лучшая точность
   - Unified для изображений и видео

4. **Advanced features** ⚡
   - Multi-object tracking
   - Negative prompts
   - Session management
   - Batched inference

### Для VFX индустрии:

**SAM 3 меняет правила игры:**

- ❌ Больше не нужно рисовать bbox
- ❌ Больше не нужно искать объекты в кадре
- ✅ Просто описываете что нужно
- ✅ Модель понимает и находит
- ✅ Экономия времени до 70%

### Планы интеграции в NukeSamurai:

**Краткосрочные (1 месяц):**
- ✅ Тестирование SAM 3
- ✅ Базовая интеграция
- ✅ Text prompts в UI
- ✅ Release v2.0

**Среднесрочные (2-3 месяца):**
- ✅ Multi-object support
- ✅ Visual exemplar prompts
- ✅ Interactive refinement
- ✅ Оптимизация производительности

**Долгосрочные (6+ месяцев):**
- ✅ Custom fine-tuning для VFX
- ✅ Integration с другими Nuke tools
- ✅ Real-time preview
- ✅ Cloud processing support

---

## 📚 Ресурсы

### Официальные:

- **GitHub:** https://github.com/facebookresearch/sam3
- **Сайт:** https://ai.meta.com/sam3/
- **HuggingFace:** https://huggingface.co/facebook/sam3
- **Paper:** (ожидается)

### Датасеты:

- **SA-Co/Gold:** https://huggingface.co/datasets/facebook/saco-gold
- **SA-Co/Silver:** https://huggingface.co/datasets/facebook/saco-silver
- **SA-Co/VEval:** https://huggingface.co/datasets/facebook/saco-veval

### Примеры:

- `examples/sam3_image_predictor_example.ipynb`
- `examples/sam3_video_predictor_example.ipynb`
- `examples/sam3_agent.ipynb`
- `examples/sam3_image_batched_inference.ipynb`

---

## 🙏 Благодарности

**Meta AI** за разработку и открытый выпуск SAM 3.

**NukeSamurai community** за поддержку и feedback.

---

## 📝 Лицензия

SAM 3 распространяется под **Apache 2.0 License**.

---

**Дата публикации:** 20 ноября 2025  
**Автор:** NukeSamurai Development Team  
**Версия:** 1.0

